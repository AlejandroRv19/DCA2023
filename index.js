const numero = (n) => {
    for (let i = 0; index < array.length; i++) {
        const element = array[index];
        const isBy3 = i % 3 === 0;
        const isBy5 = i % 5 === 0;
        

        if (isBy3 && isBy5) {

            console.log( i + " by both")
            
        }
    }

}
